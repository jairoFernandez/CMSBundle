{% extends 'base.html.twig' %}

{% block body %}
    <h1>Submenu</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ subMenu.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ subMenu.name }}</td>
            </tr>
            <tr>
                <th>Isactive</th>
                <td>{% if subMenu.isActive %}Yes{% else %}No{% endif %}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('submenu_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('submenu_edit', { 'id': subMenu.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
